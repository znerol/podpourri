[Service]

# GIT_AUTHOR_NAME:
# See man:git(1)
#
#Environment=GIT_AUTHOR_NAME=Podpourri

# GIT_AUTHOR_EMAIL:
# See man:git(1)
#
#Environment=GIT_AUTHOR_EMAIL=podpourri@%H

# GIT_COMMITTER_NAME:
# See man:git(1)
#
#Environment=GIT_COMMITTER_NAME=Podpourri

# GIT_COMMITTER_EMAIL:
# See man:git(1)
#
#Environment=GIT_COMMITTER_EMAIL=podpourri@%H

# GAU_CLONE_ARGS:
# Specify additional arguments for the git clone command.
# Defaults to --quiet.
# See man:git-gau-exec(1)
#
#Environment="GAU_CLONE_ARGS=--quiet"

# GAU_PUSH_ARGS:
# Specify additional arguments for the git push command.
# Defaults to --quiet.
# See man:git-gau-exec(1)
#
#Environment="GAU_PUSH_ARGS=--quiet"
